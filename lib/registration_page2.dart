import 'package:flutter/material.dart';

class RegStep2 extends StatelessWidget {
  const RegStep2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  Center(
        child: Text('Registration Page 2'),
      ),
    );
  }
}

